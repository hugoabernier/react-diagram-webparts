/**
 * This function is used as a stub for the PnP telemetry options.
 * We do this so that if we submit the controls to PnP property controls, the code
 * doesn't have to change -- thus reducing changes of introducing bugs.
 * @param componentName
 * @param properties
 */
export function track(componentName: string, properties: any = {}): void {
  // do nothing
}
